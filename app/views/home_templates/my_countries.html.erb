<h2>My virtual passport </h2>
<br>

<table class="table table-striped table-bordered"> <!--Figure out how to make the table striped. Bootstrap not recognizing odd or even rows -->
  <thead class="thead-light">
    <tr>
      <th scope="col">Country Code</th>
      <th scope="col">Country</th>
      <th scope="col">Added On</th>
      <th scope="col"> </th>
    </tr>
  </thead>


  <tbody>
  <%Country.all.each do |country|%>
    <tr>
      <th scope="row"><%=country.country_code%></th>
      <td><%=country.name%> </td>
      <td><%=country.created_at%></td>
      <td><a href="home/<%=country.name%>/info">Show details <a></td>
    <tr>
  <%end%>

  </tbody>

</table>

<%#create an a href tag that saves the most recent country from cookies%>